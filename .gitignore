# Go
bin/
dist/
*.exe
*.test
*.out
go.work.sum

# Rust
target/

# protocol/gen is committed on purpose: the repository must build without the
# protobuf toolchain installed. CI verifies it matches `buf generate` output.

# Python (tooling / evaluation only, never on the transaction path)
__pycache__/
.venv/
*.pyc

# Editors / OS
.idea/
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db

# Local secrets and environment overrides
.env
.env.*
!.env.example
*.local.yaml
